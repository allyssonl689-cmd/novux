# URL base da API do backend (sem barra final)
VITE_API_URL=http://localhost:3001

# Login com Google (opcional) — Client ID do OAuth (tipo "Aplicativo Web").
# DEVE ser o MESMO valor do GOOGLE_CLIENT_ID do backend. Vazio = botão do Google não aparece.
VITE_GOOGLE_CLIENT_ID=
